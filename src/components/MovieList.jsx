
// import React, { Component } from 'react'

const MovieList = () => {
	return (
		<div>
			{/* {props.movies.results &&
            props.movies?.results[0].map((movie) => (
				<div className='image-container d-flex justify-content-start m-3'
                key="id">
					{movie}
				</div>
            )  )} */}
		</div>
	);
};

export default MovieList;